name: O-RAN Near-RT RIC CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  trigger-fixed-workflow:
    uses: ./.github/workflows/main-ci-fixed.yml
