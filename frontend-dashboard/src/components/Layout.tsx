// Placeholder for Layout component
import React from 'react';

const Layout: React.FC<{ children: React.ReactNode, networkFunctions: any, connectionStatus: any, isDiscovering: any }> = ({ children }) => {
  return <div>{children}</div>;
};

export default Layout;
